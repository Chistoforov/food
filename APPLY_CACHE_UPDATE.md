# Инструкция по применению обновления кэша переводов

## Что изменилось?

Добавлена система кэширования переводов названий продуктов для решения проблемы:
- Один и тот же товар из разных чеков теперь всегда получает **одинаковый перевод**
- Используется **единая категория** (`product_type`) для группировки похожих товаров

## Быстрое применение

### Шаг 1: Применить миграцию БД

#### Через Supabase Dashboard (рекомендуется):

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (левое меню)
4. Нажмите **New Query**
5. Скопируйте всё содержимое файла `migration_add_product_name_cache.sql`
6. Вставьте в редактор
7. Нажмите **Run** (или Ctrl+Enter)
8. Дождитесь сообщения об успешном выполнении

#### Через psql (альтернатива):

```bash
psql -h <your-db-host> -U postgres -d postgres -f migration_add_product_name_cache.sql
```

### Шаг 2: Деплой обновленного кода

#### Если используете Vercel:

```bash
# Из корневой папки проекта
git add .
git commit -m "Add product name cache system"
git push origin main

# Vercel автоматически задеплоит изменения
```

#### Локальный запуск для тестирования:

```bash
# Установить зависимости (если нужно)
npm install

# Запустить dev сервер
npm run dev
```

### Шаг 3: Проверка работы

1. **Загрузите тестовый чек**
2. **Проверьте логи** в консоли браузера или на Vercel:
   ```
   📝 Cached new translation: "MILK 1L" → "Молоко 1L" (молоко)
   ```
3. **Загрузите тот же чек повторно** (или чек с тем же товаром)
4. **Проверьте, что используется кэш**:
   ```
   ✅ Using cached translation: "MILK 1L" → "Молоко 1L" (молоко)
   ```

## Проверка в базе данных

### Проверить, что таблица создана:

```sql
SELECT * FROM product_name_cache LIMIT 5;
```

**Ожидаемый результат:** Таблица существует и содержит записи из существующих продуктов.

### Проверить количество кэшированных переводов:

```sql
SELECT COUNT(*) as cache_entries FROM product_name_cache;
```

### Посмотреть самые популярные товары:

```sql
SELECT 
  original_name,
  translated_name,
  product_type,
  usage_count
FROM product_name_cache
ORDER BY usage_count DESC
LIMIT 10;
```

## Что произойдет после обновления?

### Для существующих продуктов:
- ✅ Автоматически созданы записи в кэше из таблицы `products`
- ✅ Все оригинальные названия уже в кэше
- ✅ Никаких дубликатов не будет

### Для новых чеков:
- ✅ Товары с известными названиями используют кэш
- ✅ Новые товары переводятся AI и сохраняются в кэш
- ✅ Повторные товары всегда получают одинаковый перевод

## Откат изменений (если что-то пошло не так)

### Откатить миграцию БД:

```sql
-- Удалить таблицу кэша
DROP TABLE IF EXISTS product_name_cache CASCADE;

-- Удалить функции
DROP FUNCTION IF EXISTS normalize_product_name(VARCHAR);
DROP FUNCTION IF EXISTS get_cached_translation(VARCHAR, INTEGER);
DROP FUNCTION IF EXISTS save_translation_cache(VARCHAR, VARCHAR, VARCHAR, INTEGER);
```

### Откатить код:

```bash
git revert HEAD
git push origin main
```

## Часто задаваемые вопросы

### Q: Нужно ли что-то делать с существующими продуктами?
**A:** Нет, миграция автоматически создаст записи кэша для всех существующих продуктов.

### Q: Что если AI неправильно перевел товар?
**A:** Вы можете исправить перевод в таблице `product_name_cache`:
```sql
UPDATE product_name_cache
SET translated_name = 'Правильное название',
    product_type = 'правильная_категория'
WHERE original_name = 'ORIGINAL NAME';
```

### Q: Как очистить кэш и начать заново?
**A:** 
```sql
-- Очистить весь кэш
TRUNCATE TABLE product_name_cache;

-- Или только для вашей семьи
DELETE FROM product_name_cache WHERE family_id = 1;
```

### Q: Влияет ли это на производительность?
**A:** Минимально. Добавляется один дополнительный запрос к БД при обработке каждого товара, но это компенсируется отсутствием дубликатов.

### Q: Что если два товара с одинаковым original_name на самом деле разные?
**A:** Это маловероятно для чеков из одного магазина. Но если такое произошло, можно:
1. Удалить запись из кэша для этого товара
2. При следующей обработке AI даст новый перевод
3. Или вручную добавить отличительный признак в original_name

## Мониторинг работы системы

### Dashboard query - Статистика кэша:

```sql
SELECT 
  COUNT(*) as total_cached_products,
  SUM(usage_count) as total_cache_hits,
  AVG(usage_count) as avg_usage_per_product,
  MAX(usage_count) as most_popular_product_count
FROM product_name_cache
WHERE family_id = <your_family_id>;
```

### Найти товары без кэша (не должно быть для старых товаров):

```sql
SELECT 
  p.name,
  p.original_name
FROM products p
LEFT JOIN product_name_cache pnc 
  ON LOWER(TRIM(p.original_name)) = pnc.normalized_original
  AND p.family_id = pnc.family_id
WHERE p.original_name IS NOT NULL
  AND pnc.id IS NULL;
```

## Следующие шаги

После успешного применения обновления:

1. ✅ Протестируйте на нескольких чеках
2. ✅ Проверьте, что дубликаты не создаются
3. ✅ Убедитесь, что категории (`product_type`) работают правильно
4. ✅ Проверьте логи для понимания работы системы

## Поддержка

Если возникли проблемы:
1. Проверьте логи Vercel Functions
2. Проверьте логи в консоли браузера
3. Проверьте таблицу `product_name_cache` в Supabase
4. См. подробную документацию в `PRODUCT_NAME_CACHE.md`

## Файлы обновления

- ✅ `migration_add_product_name_cache.sql` - миграция БД
- ✅ `api/process-receipt.js` - обновленная логика обработки
- ✅ `PRODUCT_NAME_CACHE.md` - полная документация
- ✅ `APPLY_CACHE_UPDATE.md` - эта инструкция

