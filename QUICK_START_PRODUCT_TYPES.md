# ⚡ Быстрый старт: Типы продуктов

## 🎯 Что это дает?

**До:** Молоко "Простоквашино" и "Parmalat" — разные продукты → отдельные прогнозы

**После:** Оба имеют тип "молоко" → общий прогноз (точнее на 40-60%!) 🚀

## 📋 Шаги для запуска

### 1️⃣ Применить миграцию в Supabase

1. Откройте Supabase SQL Editor
2. Выполните файл `migration_add_product_type.sql`:

```sql
ALTER TABLE products ADD COLUMN product_type VARCHAR(255);
CREATE INDEX idx_products_product_type ON products(product_type);
CREATE INDEX idx_products_family_product_type ON products(family_id, product_type);
```

### 2️⃣ Перезапустить приложение

```bash
npm run dev
```

### 3️⃣ Загрузить чеки

AI автоматически определит типы продуктов! 🤖

### 4️⃣ Обработать старые чеки (если есть) 🔄

**Если у вас уже были загружены чеки ДО обновления:**

1. Откройте вкладку "Продукты" 🛒
2. Нажмите кнопку **"AI Типы"** (справа вверху)
3. Подтвердите и подождите 1-5 минут
4. Готово! Все старые чеки получат типы! ✨

**Подробнее:** `REPROCESS_RECEIPTS_GUIDE.md`

### 5️⃣ (Опционально) Проверить и отредактировать типы

1. Откройте вкладку "Продукты" 🛒
2. Найдите продукт
3. Нажмите ✏️ возле "Тип продукта"
4. Отредактируйте при необходимости
5. Сохраните ✓

## ✅ Готово!

Теперь прогнозы работают с учетом типов продуктов!

---

## 📖 Подробнее

См. `PRODUCT_TYPES_GUIDE.md` для полной документации.

## 🎨 Примеры правильных типов

- `молоко` — для всех видов молока
- `хлеб белый` / `хлеб черный` — для разных видов хлеба
- `сыр плавленный` / `сыр твердый` — для разных видов сыра
- `яблоки`, `апельсины` — для фруктов
- `йогурт`, `творог` — для молочки

**Главное правило:** БЕЗ брендов, только общая категория!

✅ `молоко` — правильно
❌ `молоко простоквашино` — неправильно

