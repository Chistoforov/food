# 🧪 Как протестировать сохранение вкладок при обновлении

## 📱 Быстрый тест

### На компьютере (Chrome/Firefox/Safari)

1. **Запустите приложение**
   ```bash
   npm run dev
   ```

2. **Откройте в браузере** `http://localhost:5173`

3. **Протестируйте**:
   - Перейдите в раздел "Чек" (кликните на иконку камеры внизу)
   - Нажмите `F5` или `Cmd+R` (macOS) / `Ctrl+R` (Windows/Linux)
   - ✅ **Результат**: Вы должны остаться в разделе "Чек"

4. **Проверьте другие разделы**:
   - Перейдите в "Продукты" → Обновите страницу → Должны остаться в "Продукты"
   - Перейдите в "Аналитика" → Обновите страницу → Должны остаться в "Аналитика"
   - Перейдите на "Главная" → Обновите страницу → Останетесь на "Главная" (без уведомления)

### В PWA на телефоне (iOS/Android)

#### Установка PWA

**iOS (Safari):**
1. Откройте приложение в Safari
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх)
3. Выберите "На экран Домой"
4. Нажмите "Добавить"

**Android (Chrome):**
1. Откройте приложение в Chrome
2. Нажмите меню (три точки)
3. Выберите "Добавить на главный экран"
4. Нажмите "Добавить"

#### Тестирование

1. **Откройте PWA** с домашнего экрана

2. **Перейдите в раздел "Чек"**

3. **Pull-to-refresh**: Потяните экран вниз (как в Instagram или Twitter)
   - Экран должен обновиться
   - Вы должны остаться в разделе "Чек"
   - Должно появиться уведомление: "Раздел восстановлен после обновления"

4. **Повторите для других разделов**

## 🔍 Проверка в консоли разработчика

### Откройте консоль браузера

**Chrome/Firefox**: `F12` или `Cmd+Option+I` (macOS)  
**Safari**: `Cmd+Option+C`

### Что должно быть в консоли

При переключении вкладок:
```
💾 Сохранили текущую вкладку: upload
```

При обновлении страницы:
```
🔄 Восстанавливаем вкладку из localStorage: upload
💾 Сохранили текущую вкладку: upload
```

При выгрузке страницы (закрытии/обновлении):
```
💾 Сохранили вкладку перед выгрузкой: upload
💾 Сохранили вкладку при pagehide: upload
```

### Проверка localStorage

В консоли выполните:
```javascript
localStorage.getItem('groceryTrackerActiveTab')
```

Должно вернуть: `"home"`, `"upload"`, `"products"` или `"analytics"`

## 🎯 Что должно работать

✅ **Обновление страницы (F5)** - вкладка сохраняется  
✅ **Pull-to-refresh в PWA** - вкладка сохраняется  
✅ **Закрытие и открытие приложения** - вкладка сохраняется  
✅ **Уведомление о восстановлении** - показывается (кроме главной)  
✅ **Валидация вкладок** - некорректные значения игнорируются  

## ❌ Что НЕ сохраняется (пока)

❌ Выбранный месяц на главной странице  
❌ Позиция скролла в списках  
❌ Открытые модальные окна  
❌ Выбранный продукт в аналитике  

## 🐛 Возможные проблемы

### Вкладка не сохраняется

**Проверьте**:
1. LocalStorage включен в браузере (не приватный режим)
2. В консоли нет ошибок
3. Проверьте Application → Local Storage → `localhost:5173` в DevTools

### Уведомление не появляется

**Это нормально если**:
- Вы на главной странице (уведомление не показывается)
- Вы уже видели уведомление в этой сессии (показывается один раз)

**Сбросить счетчик**:
```javascript
localStorage.removeItem('groceryTrackerWasRestored')
```

### Pull-to-refresh не работает на iOS

**Убедитесь**:
- Приложение установлено как PWA (не просто вкладка в Safari)
- Вы тянете от самого верха экрана
- Вы не в режиме редактирования/ввода текста

## 📊 Отладочная информация

Для полной диагностики выполните в консоли:

```javascript
console.log({
  currentTab: localStorage.getItem('groceryTrackerActiveTab'),
  wasRestored: localStorage.getItem('groceryTrackerWasRestored'),
  allKeys: Object.keys(localStorage).filter(k => k.startsWith('groceryTracker'))
})
```

## 🚀 Деплой для тестирования

Для тестирования на реальном телефоне:

1. **Деплой на Vercel**:
   ```bash
   npm run build
   vercel --prod
   ```

2. **Откройте на телефоне** полученный URL

3. **Установите как PWA** (см. инструкции выше)

4. **Тестируйте** pull-to-refresh

## ✅ Чеклист успешного теста

- [ ] Обновление страницы (F5) сохраняет вкладку
- [ ] Pull-to-refresh в PWA сохраняет вкладку
- [ ] Появляется уведомление при восстановлении (не на главной)
- [ ] Уведомление исчезает через ~3 секунды
- [ ] В консоли видны логи сохранения/восстановления
- [ ] localStorage содержит `groceryTrackerActiveTab`
- [ ] Некорректные значения не ломают приложение

---

**Готово к тестированию!** 🎉

Если что-то не работает, проверьте консоль браузера для логов и ошибок.

