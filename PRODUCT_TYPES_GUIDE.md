# 🎯 Руководство по типам продуктов (Product Types)

## 📋 Обзор

Система типов продуктов позволяет группировать похожие продукты разных брендов для более точного прогнозирования. Например:
- **"Молоко Простоквашино"** и **"Молоко Parmalat"** → тип: **"молоко"**
- **"Сыр Дружба"** и **"Сыр Philadelphia"** → тип: **"сыр плавленный"**
- **"Хлеб бородинский"** и **"Хлеб черный"** → тип: **"хлеб черный"**

## 🚀 Установка

### 1. Применить миграцию базы данных

Выполните SQL миграцию в Supabase:

```sql
-- Файл: migration_add_product_type.sql
ALTER TABLE products
ADD COLUMN product_type VARCHAR(255);

CREATE INDEX idx_products_product_type ON products(product_type);
CREATE INDEX idx_products_family_product_type ON products(family_id, product_type);

COMMENT ON COLUMN products.product_type IS 'Generic product category (e.g., "молоко", "хлеб", "сыр плавленный") - used to group different brands of the same product type for better forecasting';
```

### 2. Перезапустить приложение

После применения миграции перезапустите приложение:

```bash
npm run dev
```

## 📊 Как работает прогнозирование с типами

### Старый алгоритм (без типов)
```
Продукт: "Молоко Простоквашино"
История: 5 покупок
Прогноз: на основе только этих 5 покупок
```

### Новый алгоритм (с типами)
```
Продукт: "Молоко Простоквашино"
Тип: "молоко"
История: 15 покупок (включая "Молоко Parmalat", "Молоко Домик в деревне" и т.д.)
Прогноз: на основе всех 15 покупок молока ✅
```

### Алгоритм расчета прогноза

1. **Система получает продукт для расчета прогноза**
2. **Проверяет наличие `product_type`:**
   - ✅ **Если есть тип**: собирает историю ВСЕХ продуктов с таким же типом
   - ❌ **Если нет типа**: использует только историю конкретного продукта

3. **Рассчитывает среднюю частоту покупок:**
   ```javascript
   avgDays = сумма_интервалов_между_покупками / количество_интервалов
   ```

4. **Предсказывает дату окончания:**
   ```javascript
   predictedEnd = lastPurchaseDate + avgDays
   ```

5. **Определяет статус:**
   - 🔴 `ending-soon` — осталось ≤ 2 дней
   - 🟢 `ok` — больше 2 дней
   - 🔵 `calculating` — недостаточно данных (< 2 покупок)

## 🎨 Использование в UI

### 1. Автоматическое определение типа (AI)

При загрузке чека AI автоматически определяет тип продукта:

```json
{
  "name": "Молоко 3.2% 1L",
  "originalName": "MILK 3.2% 1L",
  "productType": "молоко",  // ← AI автоматически определяет
  "quantity": 1,
  "unit": "шт",
  "price": 1.50,
  "calories": 640
}
```

### 2. Просмотр типа на главной странице

На главной странице типы отображаются как фиолетовые бейджи:

```
┌─────────────────────────────────────┐
│ Молоко 3.2% 1L         [🟢 В наличии]│
│ MILK 3.2% 1L                        │
│ [молоко] Куплено 3 раза             │ ← Тип продукта
└─────────────────────────────────────┘
```

### 3. Редактирование типа в разделе "Продукты"

1. Откройте вкладку **"Продукты"** (Shopping Cart icon)
2. Найдите нужный продукт
3. В секции **"Тип продукта"** нажмите кнопку редактирования (✏️)
4. Введите тип продукта в нижнем регистре (например: `молоко`, `хлеб белый`)
5. Нажмите **"Сохранить"** (✓)

**Система автоматически:**
- Пересчитает прогноз с учетом всех продуктов этого типа
- Обновит даты окончания
- Покажет уведомление об успешном обновлении

### 4. Автоматическое определение типов для старых чеков 🔄 NEW!

Если у вас уже есть загруженные чеки **до** внедрения системы типов:

1. Откройте вкладку **"Продукты"**
2. Нажмите кнопку **"AI Типы"** (в правом верхнем углу)
3. Подтвердите повторную обработку
4. Дождитесь завершения (1-5 минут)
5. Готово! Все старые чеки получат типы продуктов автоматически! 🎉

**Подробнее:** См. `REPROCESS_RECEIPTS_GUIDE.md`

## 📝 Примеры правильных типов

### ✅ Правильно (общая категория)

| Продукт | Правильный тип |
|---------|----------------|
| Молоко Простоквашино 3.2% | `молоко` |
| Молоко Parmalat 1.5% | `молоко` |
| Хлеб бородинский | `хлеб черный` |
| Хлеб белый нарезной | `хлеб белый` |
| Сыр Дружба плавленный | `сыр плавленный` |
| Philadelphia крем-сыр | `сыр плавленный` |
| Яблоки Гренни Смит | `яблоки` |
| Яблоки Фуджи | `яблоки` |
| Йогурт Активия | `йогурт` |
| Йогурт Danone | `йогурт` |

### ❌ Неправильно (слишком специфично)

| Продукт | Неправильный тип | Почему? |
|---------|------------------|---------|
| Молоко Простоквашино | `молоко простоквашино` | Включает бренд |
| Яблоки Гренни Смит | `яблоки гренни смит` | Включает сорт |
| Хлеб белый | `хлеб белый нарезной` | Слишком конкретно |

## 🔧 Технические детали

### Новые функции API

#### `getProductTypeHistory(productType, familyId)`
Получает историю всех продуктов с одинаковым типом.

```typescript
const history = await SupabaseService.getProductTypeHistory('молоко', 1);
// Вернет: [
//   { product_id: 1, date: '2025-01-01', quantity: 1, ... }, // Простоквашино
//   { product_id: 2, date: '2025-01-05', quantity: 1, ... }, // Parmalat
//   { product_id: 1, date: '2025-01-10', quantity: 1, ... }, // Простоквашино
// ]
```

#### `calculateProductStats(productId, familyId)`
Обновленная функция, которая автоматически выбирает историю:

```typescript
// Если у продукта есть product_type → использует групповую историю
// Если нет product_type → использует индивидуальную историю
const stats = await SupabaseService.calculateProductStats(productId, familyId);
```

### Структура базы данных

```sql
products {
  id: integer
  name: varchar(255)
  original_name: varchar(255)
  product_type: varchar(255)  -- ← НОВОЕ ПОЛЕ
  last_purchase: date
  avg_days: integer
  predicted_end: date
  status: varchar(20)
  ...
}
```

### Индексы для производительности

```sql
-- Быстрый поиск по типу
CREATE INDEX idx_products_product_type ON products(product_type);

-- Быстрый поиск по семье и типу
CREATE INDEX idx_products_family_product_type ON products(family_id, product_type);
```

## 🎯 Рекомендации по использованию

### 1. Когда указывать тип продукта

✅ **Указывайте тип для:**
- Регулярных покупок (молоко, хлеб, яйца)
- Продуктов, которые вы покупаете разных брендов
- Категорий, где важна общая частота (фрукты, овощи)

❌ **Не указывайте тип для:**
- Уникальных продуктов (конкретный деликатес)
- Продуктов, которые всегда одного бренда
- Редких покупок

### 2. Правила именования типов

1. **Всегда в нижнем регистре**: `молоко`, не `Молоко`
2. **Без брендов**: `хлеб`, не `хлеб бородинский`
3. **Можно уточнять**: `хлеб белый` vs `хлеб черный`
4. **Используйте единообразие**: всегда `молоко`, а не иногда `молочко`

### 3. Оптимальная стратегия

**Шаг 1:** Загрузите несколько чеков — AI автоматически определит типы

**Шаг 2:** Проверьте типы в разделе "Продукты"

**Шаг 3:** Скорректируйте типы вручную при необходимости:
- Объедините похожие продукты
- Разделите слишком широкие категории

**Шаг 4:** Наслаждайтесь улучшенными прогнозами! 🎉

## 🐛 Устранение неполадок

### Прогноз не обновляется

1. Проверьте, что у продукта указан `product_type`
2. Убедитесь, что есть хотя бы 2 покупки продуктов этого типа
3. Нажмите "Обновить статистику" на главной странице

### Прогноз стал менее точным

Возможно, тип продукта слишком широкий. Например:
- Было: `хлеб` (включает белый и черный)
- Стало: `хлеб белый` и `хлеб черный` (более точно)

### AI неправильно определил тип

Просто отредактируйте тип вручную:
1. Вкладка "Продукты"
2. Найдите продукт
3. Нажмите ✏️ возле "Тип продукта"
4. Введите правильный тип
5. Сохраните ✓

## 📈 Ожидаемые улучшения

После внедрения типов продуктов:

- ✅ **Точность прогнозов**: +40-60% для регулярных покупок
- ✅ **Минимальные данные**: прогноз работает быстрее (больше истории)
- ✅ **Гибкость**: можно менять бренды без потери истории
- ✅ **Интеллект**: система понимает, что "молоко" — это "молоко"

## 🎓 Примеры использования

### Сценарий 1: Переход на другой бренд молока

**До:**
```
Простоквашино: 5 покупок → прогноз: каждые 7 дней
Parmalat: 0 покупок → статус: "calculating" ❌
```

**После (с типом "молоко"):**
```
Простоквашино: тип "молоко" → прогноз: каждые 7 дней
Parmalat: тип "молоко" → прогноз: каждые 7 дней ✅
(используется общая история 5 покупок)
```

### Сценарий 2: Разные виды хлеба

```
Хлеб бородинский → тип: "хлеб черный"
Хлеб дарницкий → тип: "хлеб черный"
Батон нарезной → тип: "хлеб белый"
```

Система правильно предскажет частоту для каждого вида хлеба отдельно! 🎯

---

## 💬 Поддержка

Если возникли вопросы или проблемы:
1. Проверьте этот документ
2. Посмотрите логи в консоли браузера (F12)
3. Создайте issue на GitHub

**Приятного использования! 🛒**

