# Исправление логики определения статусов типов продуктов

## 🐛 Проблема

### Старая (неправильная) логика:
> "Если хотя бы 1 продукт типа "заканчивается" → весь тип "Заканчивается""

### Почему это плохо:
Если вы купили **батон** (тип: хлеб), он закончился, потом вы купили **белый хлеб** (тип: хлеб), то тип "Хлеб" будет **вечно гореть** как "заканчивается" из-за батона, даже если белый хлеб в наличии.

## ✅ Решение

### Новая (правильная) логика:

1. **Если есть хоть один продукт типа, купленный недавно (< 2 дней) → тип "В наличии"**
2. **Если все продукты типа куплены давно (≥ 2 дней) И прогноз показывает окончание ≤ 2 дней → тип "Заканчивается"**
3. **Если недостаточно данных (< 2 покупок типа) → тип "Расчет..."**

## 📐 Как это работает

### Алгоритм расчета статуса типа:

```javascript
async function calculateProductTypeStatus(productType, familyId) {
  // 1. Получить все продукты этого типа
  const products = await getProductsByType(productType, familyId);
  
  // 2. Получить историю ВСЕХ продуктов этого типа
  const history = await getHistoryForAllProductsOfType(productType);
  
  // 3. Проверить: есть ли недавняя покупка (< 2 дней)?
  const hasRecentPurchase = products.some(product => 
    daysSince(product.last_purchase) < 2
  );
  
  if (hasRecentPurchase) {
    return 'ok'; // ✅ Есть недавняя покупка - тип в наличии
  }
  
  // 4. Вычислить среднюю частоту покупки ТИПА
  const avgDays = calculateAverage(history);
  
  // 5. Найти последнюю покупку ЛЮБОГО продукта типа
  const lastPurchaseOfType = findLatestPurchase(products);
  
  // 6. Рассчитать прогноз окончания
  const predictedEnd = lastPurchaseOfType + avgDays;
  const daysUntilEnd = predictedEnd - today;
  
  // 7. Определить статус
  if (daysUntilEnd <= 2) {
    return 'ending-soon'; // ⚠️ До окончания ≤ 2 дней
  }
  
  return 'ok'; // ✅ Всё хорошо
}
```

## 🎯 Примеры работы

### Пример 1: Недавняя покупка типа

**Сценарий:**
- Батон: куплен 10 дней назад
- Белый хлеб: куплен **вчера** (1 день назад)

**Результат:** Тип "Хлеб" → **В наличии** ✅

**Почему:**
Есть продукт типа (белый хлеб), купленный недавно (< 2 дней)

---

### Пример 2: Все продукты куплены давно

**Сценарий:**
- Батон: куплен 10 дней назад
- Белый хлеб: куплен **5 дней назад** (последняя покупка типа)
- История покупок хлеба: каждые **7 дней**

**Расчет:**
- Последняя покупка типа: 5 дней назад
- Средняя частота: 7 дней
- Прогноз окончания: 5 + 7 = через 2 дня
- До окончания: **2 дня** ≤ 2 дней

**Результат:** Тип "Хлеб" → **Заканчивается** ⚠️

---

### Пример 3: Разные продукты одного типа

**История покупок:**
```
1 ноября:  Батон
8 ноября:  Белый хлеб
15 ноября: Багет
22 ноября: Батон (последняя покупка типа)
```

**Расчет частоты:**
- Интервалы: 7 дней, 7 дней, 7 дней
- Средняя частота: **7 дней**

**Статусы по дням:**

| Дата | Дней с покупки | Осталось до окончания | Статус |
|------|----------------|------------------------|--------|
| 22 ноября | 0 дней | 7 дней | В наличии ✅ (правило 2-х дней) |
| 23 ноября | 1 день | 6 дней | В наличии ✅ (правило 2-х дней) |
| 24 ноября | 2 дня | 5 дней | В наличии ✅ |
| 25 ноября | 3 дня | 4 дня | В наличии ✅ |
| 28 ноября | 6 дней | 1 день | **Заканчивается** ⚠️ |

---

### Пример 4: Смена продукта (ваш кейс)

**Сценарий:**
Вы покупали **батон**, он закончился, теперь покупаете **белый хлеб**.

**День 1:**
```
- Батон: куплен 10 дней назад (закончился)
- Белый хлеб: НЕ КУПЛЕН

Статус: Хлеб → Заканчивается ⚠️
```

**День 2 (покупаете белый хлеб):**
```
- Батон: куплен 10 дней назад
- Белый хлеб: куплен СЕГОДНЯ

Статус: Хлеб → В наличии ✅
Причина: Есть недавняя покупка типа (белый хлеб)
```

**День 3-4:**
```
- Батон: куплен 11-12 дней назад
- Белый хлеб: куплен 1-2 дня назад

Статус: Хлеб → В наличии ✅
Причина: Правило 2-х дней
```

**День 5+ (через несколько дней после покупки белого хлеба):**
```
- Батон: куплен давно (не влияет)
- Белый хлеб: куплен 5 дней назад (последняя покупка типа)
- Средняя частота покупки хлеба: 7 дней

Прогноз: 5 + 7 = через 2 дня
Статус: Хлеб → Заканчивается ⚠️
```

## 🔑 Ключевые моменты

### 1. Правило приоритета недавних покупок
> **Если есть хоть один продукт типа, купленный недавно (< 2 дней), тип ВСЕГДА "В наличии"**

Это решает проблему, когда вы только что купили белый хлеб, но тип показывает "заканчивается" из-за старого батона.

### 2. Расчет по типу, а не по продукту
> **Система рассчитывает частоту покупки ТИПА продукта, а не конкретного продукта**

Пример:
- Не важно, какой именно хлеб вы покупаете (батон, белый, багет)
- Важно, как часто вы покупаете **хлеб вообще**

### 3. Последняя покупка типа
> **Для прогноза используется последняя покупка ЛЮБОГО продукта типа**

Если вы купили белый хлеб вчера, а батон 10 дней назад, то:
- Последняя покупка типа = вчера (белый хлеб)
- Батон не влияет на статус

## 📊 Изменения в коде

### supabaseService.ts

**Добавлен новый метод:**
```typescript
static async calculateProductTypeStatus(
  productType: string, 
  familyId: number
): Promise<'ending-soon' | 'ok' | 'calculating'>
```

**Обновлен метод:**
```typescript
static async getProductTypeStats(familyId: number): Promise<Record<string, {
  status: 'ending-soon' | 'ok' | 'calculating'
  productCount: number
}>>
```

### GroceryTrackerApp.tsx

**Обновлена структура данных:**
```typescript
// Было:
{
  total: number
  endingSoon: number
  ok: number
  calculating: number
}

// Стало:
{
  status: 'ending-soon' | 'ok' | 'calculating'
  productCount: number
}
```

## ✅ Результат

### Теперь система:

1. ✅ **Не показывает "заканчивается" для типа, если вы только что купили продукт этого типа**
2. ✅ **Учитывает частоту покупки ТИПА, а не конкретного продукта**
3. ✅ **Позволяет покупать разные продукты одного типа без проблем**
4. ✅ **Не будет вечно гореть "заканчивается" из-за одного старого продукта**

## 🚀 Тестирование

### Сценарий для проверки:

1. **Создайте продукты типа "Хлеб":**
   - Батон
   - Белый хлеб

2. **Купите батон сегодня**
   - Статус типа "Хлеб": В наличии ✅

3. **Подождите 10 дней (не покупайте хлеб)**
   - Статус типа "Хлеб": Заканчивается ⚠️

4. **Купите белый хлеб**
   - Статус типа "Хлеб": В наличии ✅ (сразу после покупки!)

5. **Подождите 2 дня**
   - Статус типа "Хлеб": В наличии ✅ (правило 2-х дней)

6. **Подождите еще несколько дней**
   - Статус типа "Хлеб": Заканчивается ⚠️ (если прошло достаточно времени)

## 📝 Сравнение логик

| Ситуация | Старая логика | Новая логика |
|----------|---------------|--------------|
| Батон куплен 10 дней назад, белый хлеб куплен вчера | ⚠️ Заканчивается (из-за батона) | ✅ В наличии (белый хлеб недавно) |
| Все продукты типа куплены давно | ⚠️ Заканчивается | ⚠️ Заканчивается (правильно) |
| Только что купили продукт типа | ⚠️ Может показать "заканчивается" | ✅ В наличии (правило 2-х дней) |
| Покупаете разные продукты типа | ⚠️ Вечно "заканчивается" | ✅ Учитывает частоту типа |

---

**Дата исправления:** 4 ноября 2024  
**Версия:** 2.0 (исправленная логика)

