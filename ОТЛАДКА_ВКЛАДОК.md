# 🔍 Отладка проблемы с вкладками

## 🚨 Проблема

При обновлении страницы перекидывает на главную, хотя иногда появляется уведомление о восстановлении.

## ✅ Что я сделал

Добавил детальное логирование для отслеживания всех операций с вкладками:

- `[INIT]` - инициализация при загрузке
- `[MOUNT]` - монтирование компонента
- `[SAVE]` - сохранение в localStorage
- `[CHANGE]` - переключение вкладки пользователем

## 🧪 Как протестировать СЕЙЧАС

### Шаг 1: Откройте консоль браузера

1. Откройте приложение: `http://localhost:5183/` (или ваш текущий порт)
2. Нажмите `F12` (или `Cmd+Option+I` на Mac)
3. Перейдите на вкладку **Console**

### Шаг 2: Очистите localStorage

В консоли выполните:
```javascript
localStorage.clear()
```

Затем обновите страницу (`F5` или `Cmd+R`)

### Шаг 3: Тест переключения вкладки

1. **В консоли должно появиться:**
   ```
   🔄 [INIT] Восстанавливаем вкладку из localStorage: null
   ⚠️ [INIT] Вкладка невалидна или отсутствует, используем home. SavedTab: null
   🏠 [INIT] Возвращаем home по умолчанию
   🚀 [MOUNT] Компонент смонтирован, текущая вкладка: home
   📦 [MOUNT] Значение в localStorage: null
   💾 [SAVE] Сохраняем текущую вкладку в localStorage: home
   ✅ [SAVE] Вкладка сохранена успешно
   ```

2. **Кликните на вкладку "Чек" (иконка камеры)**
   
   В консоли должно появиться:
   ```
   🔄 [CHANGE] Переключаем вкладку: {from: 'home', to: 'upload', timestamp: '...'}
   💾 [SAVE] Сохраняем текущую вкладку в localStorage: upload
   ✅ [SAVE] Вкладка сохранена успешно
   ```

3. **Проверьте localStorage вручную:**
   
   В консоли выполните:
   ```javascript
   localStorage.getItem('groceryTrackerActiveTab')
   ```
   
   Должно вернуть: `"upload"`

### Шаг 4: Обновите страницу

1. **Нажмите F5 (или Cmd+R)**

2. **В консоли должно появиться:**
   ```
   🔄 [INIT] Восстанавливаем вкладку из localStorage: upload
   ✅ [INIT] Вкладка валидна, восстанавливаем: upload
   🚀 [MOUNT] Компонент смонтирован, текущая вкладка: upload
   📦 [MOUNT] Значение в localStorage: upload
   💾 [SAVE] Сохраняем текущую вкладку в localStorage: upload
   ✅ [SAVE] Вкладка сохранена успешно
   ```

3. **Проверьте, что вы на вкладке "Чек"**
   - Иконка камеры должна быть синей (активной)
   - Содержимое должно быть страницей загрузки чека

## ❓ Что искать в логах

### ✅ ПРАВИЛЬНОЕ поведение

После обновления страницы в консоли:
```
🔄 [INIT] Восстанавливаем вкладку из localStorage: upload
✅ [INIT] Вкладка валидна, восстанавливаем: upload
🚀 [MOUNT] Компонент смонтирован, текущая вкладка: upload
```

И вы на странице "Чек".

### ❌ НЕПРАВИЛЬНОЕ поведение

Если в консоли:
```
🔄 [INIT] Восстанавливаем вкладку из localStorage: upload
✅ [INIT] Вкладка валидна, восстанавливаем: upload
🚀 [MOUNT] Компонент смонтирован, текущая вкладка: home  ← ЭТО ПРОБЛЕМА!
```

Это означает, что между инициализацией и монтированием что-то меняет вкладку на `home`.

## 🔍 Дополнительная диагностика

### Проверка 1: Какие ключи в localStorage?

```javascript
Object.keys(localStorage).filter(k => k.includes('grocery'))
```

Должно показать: `["groceryTrackerActiveTab"]`

### Проверка 2: Полная информация

```javascript
console.log({
  activeTab: localStorage.getItem('groceryTrackerActiveTab'),
  wasRestored: localStorage.getItem('groceryTrackerWasRestored'),
  allKeys: Object.keys(localStorage)
})
```

### Проверка 3: Отследить все изменения localStorage

```javascript
// Вставьте в консоль ДО обновления страницы
const originalSetItem = localStorage.setItem;
localStorage.setItem = function(key, value) {
  if (key.includes('grocery')) {
    console.trace('📝 localStorage.setItem вызван:', key, '=', value);
  }
  return originalSetItem.apply(this, arguments);
};
```

Затем обновите страницу и посмотрите стек вызовов.

## 📋 Чеклист для отчета

Пожалуйста, проверьте и сообщите:

- [ ] Что показывается в консоли при загрузке страницы?
- [ ] Что показывается в консоли при переключении на "Чек"?
- [ ] Что показывается в консоли при обновлении страницы?
- [ ] Какое значение в `localStorage.getItem('groceryTrackerActiveTab')` перед обновлением?
- [ ] Какое значение в `localStorage.getItem('groceryTrackerActiveTab')` после обновления?
- [ ] На какой вкладке вы видите интерфейс после обновления?
- [ ] Какая иконка внизу подсвечена синим?

## 🐛 Возможные причины

### Причина 1: localStorage очищается
Если перед обновлением `localStorage` содержит `"upload"`, но после обновления `null` - значит что-то очищает данные.

### Причина 2: Конфликт ключей
Возможно, старый код использует ключ `activeTab` вместо `groceryTrackerActiveTab`.

Проверьте:
```javascript
localStorage.getItem('activeTab')  // старый ключ
localStorage.getItem('groceryTrackerActiveTab')  // новый ключ
```

### Причина 3: React StrictMode
В development режиме React может монтировать компонент дважды. Проверьте, нет ли в консоли дублирующихся логов.

### Причина 4: Другой код меняет вкладку
Где-то в коде может быть `setActiveTab('home')` при монтировании.

## 🎯 Следующие шаги

1. **Выполните все шаги выше**
2. **Сохраните логи из консоли**
3. **Сделайте скриншот консоли** после обновления страницы
4. **Сообщите результаты**

Я буду ждать ваших результатов, чтобы точно определить причину проблемы!

---

**Важно:** Держите консоль открытой все время тестирования!

