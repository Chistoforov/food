# Система кэширования переводов названий продуктов

## Проблема

При обработке чеков Perplexity AI может переводить одно и то же оригинальное название товара по-разному в разных чеках. Например:

- Чек 1: `"MILK 3.2% 1L"` → `"Молоко 3.2% 1L"` + тип: `"молоко"`
- Чек 2: `"MILK 3.2% 1L"` → `"Молоко обезжиренное 1L"` + тип: `"молоко обезжиренное"`

Это приводит к созданию дубликатов продуктов в базе данных, несмотря на то, что речь идет об одном и том же товаре.

## Решение

Реализована система кэширования переводов, которая:

1. **Сохраняет первый перевод** каждого оригинального названия в специальную таблицу `product_name_cache`
2. **Использует кэшированный перевод** при повторном появлении того же оригинального названия
3. **Обеспечивает консистентность** - один и тот же товар всегда получает одинаковое русское название и категорию

## Как это работает

### Обработка нового чека

```
Товар из чека: "MILK 3.2% 1L"
    ↓
Проверка кэша по оригинальному названию
    ↓
    ├─→ Найдено в кэше?
    │   └─→ ✅ Используем сохраненный перевод: "Молоко 3.2% 1L" + тип: "молоко"
    │
    └─→ Не найдено?
        └─→ 🤖 Спрашиваем AI
            └─→ 💾 Сохраняем перевод в кэш для будущих чеков
            └─→ ✅ Используем новый перевод
```

### Структура таблицы `product_name_cache`

```sql
CREATE TABLE product_name_cache (
  id SERIAL PRIMARY KEY,
  original_name VARCHAR(500),           -- Оригинальное название (как есть)
  normalized_original VARCHAR(500),     -- Нормализованное для сравнения
  translated_name VARCHAR(255),         -- Русское название
  product_type VARCHAR(255),            -- Категория продукта
  family_id INTEGER,                    -- Кэш для каждой семьи отдельно
  usage_count INTEGER,                  -- Сколько раз использовался
  created_at TIMESTAMP,
  updated_at TIMESTAMP
);
```

### Нормализация названий

Для надежного сопоставления оригинальные названия нормализуются:

```javascript
"MILK 3.2% 1L"      → "milk 3.2% 1l"      (lowercase)
"  Milk   1L  "     → "milk 1l"           (trim + normalize spaces)
"Milk    3.2%"      → "milk 3.2%"         (normalize spaces)
```

## Функции базы данных

### `normalize_product_name(input_name)`
Нормализует название для сравнения (lowercase, trim, убирает лишние пробелы)

### `get_cached_translation(p_original_name, p_family_id)`
Получает кэшированный перевод для оригинального названия

**Возвращает:**
```json
{
  "translated_name": "Молоко 3.2% 1L",
  "product_type": "молоко"
}
```

### `save_translation_cache(p_original_name, p_translated_name, p_product_type, p_family_id)`
Сохраняет новый перевод в кэш. Если запись уже существует, увеличивает счетчик использований.

## Логика обработки чека

### 1. Проверка кэша
```javascript
const cachedTranslation = await getCachedTranslation(item.originalName, familyId);
```

### 2. Выбор финального названия
```javascript
const finalName = cachedTranslation 
  ? cachedTranslation.translated_name  // Используем кэш
  : item.name;                          // Используем перевод от AI

const finalProductType = cachedTranslation 
  ? cachedTranslation.product_type     // Используем кэш
  : item.productType;                   // Используем от AI
```

### 3. Сохранение в кэш (если не было)
```javascript
if (!cachedTranslation && item.originalName) {
  await saveCachedTranslation(
    item.originalName, 
    item.name, 
    item.productType, 
    familyId
  );
}
```

### 4. Поиск существующего продукта

Улучшенная логика поиска продукта в базе:

1. **Сначала по типу продукта** (`product_type`) - если совпадает, используем существующий продукт
2. **Затем по названию** (`name`) - если типа нет, ищем по имени

Это позволяет объединять покупки одного типа продукта, даже если AI дал немного разные названия.

## Преимущества

### ✅ Консистентность
Один и тот же товар всегда имеет одинаковое название и категорию

### ✅ Экономия на API
Не нужно переспрашивать AI для уже известных товаров (хотя в текущей реализации AI все равно обрабатывает весь чек)

### ✅ Контроль качества
Можно проверить и исправить переводы в таблице `product_name_cache`

### ✅ Статистика использования
Поле `usage_count` показывает, как часто встречается каждый товар

### ✅ Группировка по типу
`product_type` позволяет группировать похожие продукты разных брендов

## Примеры работы

### Пример 1: Новый товар

**Чек 1:**
```
Original: "RAMBOL CHEESE 212g"
AI перевод: "Сыр Rambol 212г"
AI тип: "сыр плавленный"

→ Сохраняем в кэш:
  - original_name: "RAMBOL CHEESE 212g"
  - normalized_original: "rambol cheese 212g"
  - translated_name: "Сыр Rambol 212г"
  - product_type: "сыр плавленный"
  - usage_count: 1
```

### Пример 2: Повторный товар

**Чек 2 (через неделю):**
```
Original: "RAMBOL CHEESE 212g"
Кэш найден! ✅

→ Используем из кэша:
  - translated_name: "Сыр Rambol 212г"
  - product_type: "сыр плавленный"

→ Обновляем usage_count: 2
```

### Пример 3: Похожий товар другого бренда

**Чек 3:**
```
Original: "PHILADELPHIA CHEESE 200g"
AI перевод: "Сыр Филадельфия 200г"
AI тип: "сыр плавленный"  ← Тот же тип!

→ Сохраняем в кэш как новый товар
→ НО находим существующий продукт по типу "сыр плавленный"
→ Добавляем покупку к существующему продукту
```

## Миграция существующих данных

При установке миграции автоматически создаются записи кэша для всех существующих продуктов:

```sql
INSERT INTO product_name_cache (...)
SELECT DISTINCT ON (normalize_product_name(original_name), family_id)
  original_name,
  normalize_product_name(original_name),
  name,
  product_type,
  family_id,
  purchase_count
FROM products
WHERE original_name IS NOT NULL;
```

## Применение миграции

### 1. Через Supabase Dashboard

1. Перейдите в SQL Editor
2. Скопируйте содержимое файла `migration_add_product_name_cache.sql`
3. Выполните SQL запрос

### 2. Через Supabase CLI

```bash
# Если используете Supabase локально
supabase db reset

# Или примените миграцию вручную
psql -h <host> -U <user> -d <database> -f migration_add_product_name_cache.sql
```

## Мониторинг и отладка

### Проверка кэша
```sql
-- Посмотреть все кэшированные переводы
SELECT 
  original_name,
  translated_name,
  product_type,
  usage_count
FROM product_name_cache
ORDER BY usage_count DESC;
```

### Самые популярные товары
```sql
-- Топ-10 самых частых товаров
SELECT 
  original_name,
  translated_name,
  product_type,
  usage_count
FROM product_name_cache
ORDER BY usage_count DESC
LIMIT 10;
```

### Товары с разными переводами (потенциальные проблемы)
```sql
-- Найти похожие оригинальные названия с разными переводами
SELECT 
  p1.original_name as name1,
  p1.translated_name as trans1,
  p2.original_name as name2,
  p2.translated_name as trans2
FROM product_name_cache p1
JOIN product_name_cache p2 
  ON p1.normalized_original = p2.normalized_original 
  AND p1.id != p2.id
WHERE p1.family_id = p2.family_id;
```

## Ручное управление кэшем

### Исправить неправильный перевод
```sql
UPDATE product_name_cache
SET 
  translated_name = 'Правильное название',
  product_type = 'правильный_тип'
WHERE original_name = 'ORIGINAL NAME FROM RECEIPT';
```

### Удалить запись из кэша (чтобы AI перевел заново)
```sql
DELETE FROM product_name_cache
WHERE original_name = 'ORIGINAL NAME FROM RECEIPT';
```

### Очистить весь кэш семьи (начать заново)
```sql
DELETE FROM product_name_cache
WHERE family_id = <your_family_id>;
```

## Логи в консоли

При обработке чека вы увидите:

```
✅ Using cached translation: "MILK 3.2% 1L" → "Молоко 3.2% 1L" (молоко)
📝 Cached new translation: "BREAD WHITE" → "Хлеб белый" (хлеб белый)
✅ Using cached translation: "APPLES" → "Яблоки" (яблоки)
```

- `✅` = использован кэшированный перевод
- `📝` = новый перевод сохранен в кэш

## Будущие улучшения

### 1. Улучшенное сопоставление
- Fuzzy matching для похожих названий (например, "MILK 1L" и "MILK 1.0L")
- Учет синонимов и сокращений

### 2. UI для управления кэшем
- Просмотр всех переводов
- Редактирование переводов через интерфейс
- Объединение дубликатов

### 3. Умная предварительная проверка
- Перед отправкой в AI проверять все товары в чеке
- Отправлять в AI только новые товары
- Экономия токенов API

### 4. Обучение на истории
- Анализ частых товаров
- Предложения по улучшению переводов
- Автоматическое исправление очевидных ошибок

## Связанные файлы

- `migration_add_product_name_cache.sql` - SQL миграция
- `api/process-receipt.js` - логика обработки чеков
- `database_schema.sql` - основная схема БД
- `migration_add_product_type.sql` - миграция добавления product_type

