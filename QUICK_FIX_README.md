# 🔧 Быстрое исправление проблем

## 📋 Обнаруженные проблемы

### 1. ❌ Ошибка при сбросе кэша
**Симптом:** При нажатии кнопки "Сброс кэша" в разделе "Продукты" появляется ошибка:
```
invalid input syntax for type integer: "2025-10"
```

**Причина:** Отсутствуют SQL функции `recalculate_family_analytics` и `recalculate_all_analytics` в базе данных.

**Решение:** Применить миграцию

### 2. ⚠️ PWA не предлагает установку
**Симптом:** Телефон не показывает предложение установить приложение.

**Возможные причины:** См. подробное руководство ниже.

---

## 🚀 Решение проблем

### Шаг 1: Исправить ошибку сброса кэша

#### Вариант А: Через Supabase Dashboard (рекомендуется)

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (левое меню)
4. Откройте файл `migration_add_recalculate_family_analytics.sql` из этой папки
5. Скопируйте весь его текст
6. Вставьте в SQL Editor
7. Нажмите **Run** (или Ctrl+Enter)
8. Дождитесь сообщения об успешном выполнении

#### Вариант Б: Быстрая проверка после миграции

Выполните в SQL Editor:

```sql
-- Проверить, что функции созданы
SELECT routine_name 
FROM information_schema.routines
WHERE routine_schema = 'public'
AND routine_name IN ('recalculate_family_analytics', 'recalculate_all_analytics');
```

Должны вернуться 2 строки с названиями функций.

#### Вариант В: Тестирование

1. Откройте приложение в браузере
2. Перейдите на вкладку **Продукты**
3. Нажмите кнопку **Сброс кэша** (красная кнопка справа вверху)
4. Убедитесь, что:
   - ✅ Ошибка больше не появляется
   - ✅ Появилось сообщение "Кэш очищен! Аналитика пересчитана."
   - ✅ Страница перезагрузилась через 2 секунды

---

### Шаг 2: Исправить установку PWA

#### Для Android (Chrome)

1. **Проверьте HTTPS**
   - Откройте сайт и убедитесь, что в адресной строке есть замочек 🔒
   - URL должен начинаться с `https://`

2. **Проверьте Service Worker**
   - Откройте Chrome на телефоне
   - Нажмите меню (три точки) → **Дополнительные инструменты** → **Инструменты разработчика**
   - Или на компьютере: откройте сайт, нажмите F12
   - Перейдите на вкладку **Application**
   - В левом меню выберите **Service Workers**
   - Должен быть статус: **activated and is running** (зеленая точка)

3. **Проверьте Manifest**
   - В той же вкладке **Application** выберите **Manifest**
   - Убедитесь, что все поля заполнены
   - Иконки должны отображаться (Preview)

4. **Установка**
   - Закройте DevTools
   - Подождите 5-10 секунд
   - Должна появиться мини-панель внизу: "Добавить Grocery на главный экран"
   - Или нажмите меню → "Установить приложение"

#### Для iOS (Safari)

**⚠️ ВАЖНО:** Safari НЕ показывает автоматическое предложение!

1. Откройте сайт в Safari
2. Нажмите кнопку **Поделиться** (□↑ квадрат со стрелкой вверх)
3. Прокрутите вниз и выберите **"На экран «Домой»"**
4. Введите название (по умолчанию "Grocery")
5. Нажмите **Добавить**

#### Быстрая диагностика

Откройте консоль браузера (F12 → Console) и выполните:

```javascript
// Проверка Service Worker
navigator.serviceWorker.getRegistrations().then(regs => {
  if (regs.length === 0) {
    console.error('❌ Service Worker не зарегистрирован');
  } else {
    console.log('✅ Service Workers:', regs.length);
    regs.forEach(reg => console.log('Scope:', reg.scope, 'Active:', !!reg.active));
  }
});

// Проверка manifest
fetch('/manifest.json')
  .then(r => r.json())
  .then(m => console.log('✅ Manifest загружен:', m))
  .catch(e => console.error('❌ Ошибка manifest:', e));

// Проверка иконок
Promise.all([
  fetch('/icon-192.png'),
  fetch('/icon-512.png')
]).then(([r192, r512]) => {
  console.log('Icon 192:', r192.ok ? '✅' : '❌');
  console.log('Icon 512:', r512.ok ? '✅' : '❌');
});
```

---

## 📚 Подробная документация

Для более детальной информации см.:

- **APPLY_MIGRATION_RECALCULATE_ANALYTICS.md** - Подробная инструкция по применению миграции
- **PWA_INSTALL_FIX.md** - Полное руководство по отладке установки PWA

---

## ✅ Чеклист проверки

После выполнения всех шагов:

- [ ] Миграция применена в Supabase
- [ ] Функции `recalculate_family_analytics` и `recalculate_all_analytics` созданы
- [ ] Кнопка "Сброс кэша" работает без ошибок
- [ ] Service Worker активен (проверено в DevTools)
- [ ] Manifest загружается (проверено в DevTools)
- [ ] Иконки доступны (проверено в консоли)
- [ ] HTTPS работает (замочек в адресной строке)
- [ ] PWA устанавливается (проверено на телефоне)

---

## 🆘 Если что-то не работает

1. **Очистите кэш браузера**
   - Chrome: Ctrl+Shift+Delete → Выбрать "Все время" → Очистить
   
2. **Удалите старый Service Worker**
   - F12 → Application → Service Workers → Unregister
   - Перезагрузите страницу (Ctrl+F5)

3. **Проверьте консоль на ошибки**
   - F12 → Console
   - Ищите красные сообщения об ошибках

4. **Запустите Lighthouse аудит**
   - F12 → Lighthouse → Progressive Web App → Generate report
   - Изучите список проблем

---

## 📞 Контакты

Если проблемы остаются, проверьте:
- Логи в консоли браузера (F12 → Console)
- Логи Service Worker (F12 → Application → Service Workers)
- Статус функций в Supabase SQL Editor

---

**Дата:** 4 ноября 2025  
**Статус:** Готово к применению

