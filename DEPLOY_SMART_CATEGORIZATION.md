# 🚀 Деплой Умной Категоризации

## 📦 Что изменено

**Файл:** `api/process-receipt.js`

**Изменения:**
1. Добавлена функция `getExistingProductTypes()` - извлекает существующие категории
2. Добавлена функция `findSimilarProductType()` - находит похожие продукты
3. Обновлена функция `parseReceiptWithPerplexity()` - принимает контекст категорий
4. Обновлен основной обработчик - использует новые функции

## ✅ Деплой на Vercel

### Вариант 1: Автоматический деплой (рекомендуется)

```bash
# 1. Закоммитить изменения
git add api/process-receipt.js
git commit -m "feat: добавлена умная категоризация продуктов"

# 2. Запушить в main
git push origin main

# 3. Vercel автоматически задеплоит изменения
```

Vercel автоматически обнаружит изменения и задеплоит их.
Проверьте статус деплоя на https://vercel.com/dashboard

### Вариант 2: Ручной деплой через CLI

```bash
# 1. Установить Vercel CLI (если еще не установлен)
npm i -g vercel

# 2. Задеплоить
vercel --prod
```

## 🧪 Тестирование

### 1. Проверка логов

После деплоя загрузите тестовый чек и проверьте логи в Vercel Dashboard:

1. Зайдите на https://vercel.com/dashboard
2. Выберите проект Grocery
3. Перейдите в Functions → process-receipt
4. Проверьте логи последнего запроса

**Ожидаемые логи:**

```
Fetching existing product types for family: [familyId]
Found existing product types, including in prompt
Parsing receipt: [receiptId]
🔍 Found similar product: "..." → "..." (...% match, type: ...)
🔄 Adjusting category from "..." to "..." based on similar product
```

### 2. Проверка категоризации

**Тест 1: Повторная покупка**
1. Купите продукт, который уже есть в базе
2. Загрузите чек
3. Убедитесь, что категория осталась той же

**Тест 2: Похожий продукт**
1. Если у вас есть "Киндер шоколад" → "киндер"
2. Загрузите чек с "Шоколад Киндер молочный"
3. Убедитесь, что категория будет "киндер", а не "шоколад"

**Тест 3: Новый тип продукта**
1. Загрузите чек с совершенно новым продуктом
2. Убедитесь, что создалась новая категория
3. Загрузите еще один чек с похожим продуктом
4. Убедитесь, что используется та же категория

## 🔍 Отладка проблем

### Проблема: Категории все еще дублируются

**Решение 1:** Проверьте логи Vercel
```
Должны быть строки:
- "Found existing product types, including in prompt"
- "Found similar product: ..."
```

Если этих строк нет - проверьте, правильно ли задеплоился код.

**Решение 2:** Проверьте базу данных
```sql
-- Убедитесь, что у продуктов есть product_type
SELECT name, product_type, purchase_count 
FROM products 
WHERE family_id = 'YOUR_FAMILY_ID'
ORDER BY purchase_count DESC
LIMIT 10;
```

**Решение 3:** Увеличьте порог похожести
Если система слишком строгая, измените порог в функции `findSimilarProductType()`:

```javascript
// Было: if (score > highestScore && score >= 0.6)
// Станет: if (score > highestScore && score >= 0.5)
```

### Проблема: Слишком много совпадений

Если система объединяет разные продукты в одну категорию, увеличьте порог:

```javascript
// Было: if (score > highestScore && score >= 0.6)
// Станет: if (score > highestScore && score >= 0.7)
```

## 📈 Мониторинг

### Метрики для отслеживания

1. **Количество уникальных категорий** (должно расти медленнее)
   ```sql
   SELECT COUNT(DISTINCT product_type) 
   FROM products 
   WHERE family_id = 'YOUR_FAMILY_ID';
   ```

2. **Процент автокоррекций** (смотрите в логах)
   - Ищите строки с "🔄 Adjusting category"

3. **Качество категоризации** (ручная проверка)
   - Периодически проверяйте новые продукты
   - Убедитесь, что похожие продукты в одной категории

## 🎯 Ожидаемые результаты

После деплоя вы должны увидеть:

- ✅ Меньше дублирующих категорий
- ✅ Автоматическая коррекция при повторных покупках
- ✅ Логи с информацией о найденных совпадениях
- ✅ Более консистентная категоризация

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в Vercel Dashboard
2. Проверьте структуру базы данных
3. Убедитесь, что изменения правильно задеплоились
4. Проверьте, что PERPLEXITY_API_KEY настроен

---

**Дата создания:** 12 ноября 2025  
**Версия:** 1.0









