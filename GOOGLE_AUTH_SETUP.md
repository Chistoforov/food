# 🔑 Настройка Google Auth для Supabase

Чтобы получить **Google Client ID** и **Client Secret**, вам нужно создать проект в Google Cloud Console. Вот подробная пошаговая инструкция:

## Шаг 1: Создание проекта в Google Cloud

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/).
2. Если у вас еще нет аккаунта, зарегистрируйтесь (это бесплатно).
3. В верхнем левом углу рядом с логотипом Google Cloud нажмите на выпадающий список проектов.
4. В открывшемся окне нажмите **New Project** (Создать проект).
5. Введите название проекта (например, `Grocery App`) и нажмите **Create**.
6. Дождитесь создания и выберите этот проект в том же выпадающем списке сверху.

## Шаг 2: Настройка экрана согласия (OAuth Consent Screen)

Перед созданием ключей нужно настроить экран, который видят пользователи при входе.

1. В левом меню перейдите в **APIs & Services** > **OAuth consent screen** (Экран согласия OAuth).
2. Выберите **External** (Внешний) — доступен для любых пользователей с Google аккаунтом.
3. Нажмите **Create**.
4. Заполните обязательные поля:
   - **App name**: Название приложения (например, `Grocery App`).
   - **User support email**: Ваш email.
   - **Developer contact information**: Ваш email.
5. Нажмите **Save and Continue**.
6. На шаге **Scopes** можно ничего не менять, просто нажмите **Save and Continue**.
7. На шаге **Test Users** пока можно ничего не добавлять. Нажмите **Save and Continue**.
8. В конце нажмите **Back to Dashboard**.

> **Важно:** Пока приложение в статусе "Testing", войти смогут только те email, которые вы добавите в разделе "Test Users". Чтобы пускать всех, позже нужно будет нажать кнопку **Publish App** (Опубликовать приложение) на экране OAuth consent screen.

## Шаг 3: Получение Client ID и Secret

1. В левом меню перейдите в **APIs & Services** > **Credentials** (Учетные данные).
2. Нажмите кнопку **+ CREATE CREDENTIALS** вверху и выберите **OAuth client ID**.
3. В поле **Application type** выберите **Web application**.
4. В поле **Name** введите название (например, `Supabase Auth`).
5. В разделе **Authorized redirect URIs** (Разрешенные URI перенаправления) нужно добавить ссылку из Supabase:
   - Откройте новую вкладку браузера.
   - Зайдите в [Supabase Dashboard](https://supabase.com/dashboard) -> Ваш проект.
   - Перейдите в **Authentication** -> **Providers** -> **Google**.
   - Скопируйте **Callback URL (for OAuth)** (выглядит как `https://<ваш-проект>.supabase.co/auth/v1/callback`).
   - Вернитесь в Google Cloud Console и нажмите **ADD URI** под "Authorized redirect URIs".
   - Вставьте скопированную ссылку.
6. Нажмите **Create**.

## Шаг 4: Подключение к Supabase

1. После нажатия Create появится окно с вашими ключами:
   - **Your Client ID**
   - **Your Client Secret**
2. Скопируйте **Client ID**.
3. Перейдите в Supabase Dashboard -> Authentication -> Providers -> Google.
4. Вставьте в поле **Client ID**.
5. Вернитесь в Google Cloud и скопируйте **Client Secret**.
6. Вставьте в поле **Client Secret** в Supabase.
7. Нажмите **Save** в Supabase.

## Шаг 5: Добавление URL приложения (для редиректа после входа)

1. В Supabase перейдите в **Authentication** -> **URL Configuration**.
2. В поле **Site URL** укажите адрес вашего приложения:
   - Для разработки: `http://localhost:5173`
   - Для продакшена (если есть): `https://your-app.vercel.app`
3. Если вы используете и localhost, и продакшн, добавьте дополнительные ссылки в **Redirect URLs**.
4. Нажмите **Save**.

---
✅ **Готово!** Теперь кнопка "Войти через Google" в вашем приложении должна работать.





