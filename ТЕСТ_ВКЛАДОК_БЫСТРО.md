# ⚡ БЫСТРЫЙ ТЕСТ - Отладка вкладок

## 🎯 Что делать ПРЯМО СЕЙЧАС

### 1. Откройте приложение

```
http://localhost:5183/
```

(приложение уже запущено, см. терминал)

### 2. Откройте консоль браузера

- Нажмите `F12` (или `Cmd+Option+I` на Mac)
- Перейдите на вкладку **Console**

### 3. Очистите localStorage

В консоли выполните:
```javascript
localStorage.clear()
```

Обновите страницу: `F5` или `Cmd+R`

### 4. Перейдите на "Чек"

- Кликните на иконку камеры внизу
- **ПОСМОТРИТЕ В КОНСОЛЬ** - должны появиться логи

### 5. Обновите страницу

- Нажмите `F5` или `Cmd+R`
- **ВНИМАТЕЛЬНО СМОТРИТЕ ЛОГИ В КОНСОЛИ**

### 6. Что должно быть в консоли?

**При переключении на "Чек":**
```
🔄 [CHANGE] Переключаем вкладку: {from: 'home', to: 'upload', ...}
💾 [SAVE] Сохраняем текущую вкладку в localStorage: upload
✅ [SAVE] Вкладка сохранена успешно
```

**После обновления страницы:**
```
🔄 [INIT] Восстанавливаем вкладку из localStorage: upload
✅ [INIT] Вкладка валидна, восстанавливаем: upload
🚀 [MOUNT] Компонент смонтирован, текущая вкладка: upload
```

### 7. Проверьте localStorage

В консоли:
```javascript
localStorage.getItem('groceryTrackerActiveTab')
```

Должно показать: `"upload"`

---

## ❓ Что сообщить мне

**Скопируйте из консоли:**
1. Все логи после переключения на "Чек"
2. Все логи после обновления страницы
3. Результат `localStorage.getItem('groceryTrackerActiveTab')`

**И ответьте:**
- На какой вкладке вы оказались после обновления? (Какая иконка синяя?)
- Показалось ли уведомление "Раздел восстановлен"?

---

## 🐛 Если не работает

### Если в консоли видите:
```
🔄 [INIT] Восстанавливаем вкладку из localStorage: upload
✅ [INIT] Вкладка валидна, восстанавливаем: upload
🚀 [MOUNT] Компонент смонтирован, текущая вкладка: home  ← ПРОБЛЕМА!
```

То между инициализацией и монтированием что-то меняет вкладку.

### Дополнительная проверка

```javascript
// Проверим старый ключ (может конфликтует)
localStorage.getItem('activeTab')

// Проверим новый ключ
localStorage.getItem('groceryTrackerActiveTab')
```

---

**ЖДУ ВАШИХ ЛОГОВ ИЗ КОНСОЛИ!** 🔍

